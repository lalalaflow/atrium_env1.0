<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybastis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.sfzen.atrium.aml.mapper.common.SampleMapper">
	<select id="getWorkArea" parameterType="String" resultType="String" >
		SELECT NAME
		FROM WORK_AREA
		WHERE CODE=#{code}
	</select>
	<select id="getWorkAreas" resultType="String" >
		SELECT NAME
		FROM WORK_AREA
	</select>
	
</mapper>